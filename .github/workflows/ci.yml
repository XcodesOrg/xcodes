name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: macos-12
    steps:
    - uses: actions/checkout@v4
    - name: Run tests
      env: 
        DEVELOPER_DIR: /Applications/Xcode_13.4.1.app
      run: swift test
